[package]
name = "__ffi_time-test"
description = "This is a test library. Do not publish it."
version = "0.0.1"
edition = "2021"
rust_version = "1.83.0"
publish = false

[build-dependencies]
cc = "1.1.10"

[dev-dependencies]
proptest = "1.5.0"
ffi_time = { path = "../" }

[dependencies]
libc = "0.2.169"
static_assertions = "1.1.0"
